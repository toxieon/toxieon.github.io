========================================================================
 NEILL DATA SUITE — REUSABLE ASSETS
 A growing library of drop-in feature components shared across the apps
 (uploader, quote, planner, timesheet, swb, hub).
 Update this file whenever an asset is added, changed, or wired into an app.
 Last updated: 2026-08-05
========================================================================

HOW THIS RELATES TO /shared
  /shared  = low-level runtime modules the apps already depend on
             (nd-auth, nd-sheets, nd-queue, nd-cache, nd-ui, nd-core.css, …).
  /assets  = higher-level, self-contained FEATURE components you can lift
             into any app with a single <script> tag. This is the library
             we grow as we pull the "best of" each app into one place.

  Load an asset by adding, before your app script:
      <script src="../assets/here.js"></script>

------------------------------------------------------------------------
 ASSETS PRESENT
------------------------------------------------------------------------

[1] here.js  —  "Here" one-tap geolocation → address
    Global:   NDHere
    Origin:   Quote (#use-location-btn)
    Needs:    navigator.geolocation + google.maps.Geocoder (host loads Maps)
    API:      NDHere.locate(opts) -> Promise<{address, lat, lng}>
              NDHere.fill(inputEl, opts)      (writes address into a field)
              NDHere.attachButton(btn, inputEl, opts)   (one-call wiring)
    Options:  region ("au"), highAccuracy, timeout, busyLabel, mapsKey,
              onResult(addr,coords), onError(err), notify(msg)
    Used by:  quote, uploader, planner, timesheet  (WIRED)
    Status:   WIRED into all four apps
    Notes:    Uses google.maps.Geocoder when the Maps JS library is loaded;
              falls back to REST geocoding via opts.mapsKey when it isn't
              (that's how Timesheet uses it). Errors surface via NDUI.toast
              when available, else alert().

[2] watermark.js  —  photo watermark info strip
    Global:   NDWatermark
    Origin:   Quote (drawWatermark)
    Needs:    Canvas 2D only (GPS lookup optional, uses geolocation)
    API:      NDWatermark.buildLines(data, toggles) -> string[]   (pure)
              NDWatermark.draw(ctx, w, h, {lines})                (sync)
              NDWatermark.stamp(source, {lines, maxDim, jpegQ}) -> dataURL
              NDWatermark.getGps(opts) -> Promise<"lat, lng"|null>
    Fields:   business, clientName, address, room, apartment, location,
              ref, datetime, initials, gps  (each individually toggleable;
              room/apt/location merge to one line; datetime + initials share
              a line, e.g. "27 Jul 2026 3:45 pm · BN")
    Toggles:  NDWatermark.DEFAULT_TOGGLES (clone + override per app)
    Used by:  uploader (baked on upload, per-field toggle panel + initials),
              quote (in-app camera capture)  — both WIRED
    Status:   WIRED. Uploader skips floor plans + HEIC; falls back to the
              original file on any error so an upload never fails.

[3] countdown-confirm.js  —  auto-confirming prompt with a depleting bar
    Global:   NDCountdownConfirm
    Origin:   Timesheet ("near" clock-on prompt)
    Needs:    nothing (self-contained; injects its own styles + DOM)
    API:      NDCountdownConfirm.open({ title, message, seconds=4,
                confirmLabel, secondaryLabel, accent,
                onConfirm, onSecondary, onCancel }) -> { close(fireCancel) }
    Behaviour: bar drains over `seconds`, then auto-fires onConfirm unless a
              button is tapped. Secondary = "change my mind" action; backdrop
              tap / Esc = cancel. "Assume yes, but give a moment to change."
    Used by:  Timesheet clock-on (pattern); available suite-wide
    Status:   BUILT

[4] exif-geo-detect.js  —  read a photo's GPS + capture time → address
    Global:   NDExifGeo
    Origin:   Uploader (EXIF auto-location)
    Needs:    exifr (window.exifr) + google.maps.Geocoder OR opts.mapsKey
    API:      NDExifGeo.readExif(file) -> { lat, lng, capturedAt }
              NDExifGeo.coordKey(lat, lng)                       (pure)
              NDExifGeo.reverseGeocode(lat, lng, { region, mapsKey })
              NDExifGeo.geocodeClusters(points, { cache, mapsKey })
                — one geocode per unique 5dp coordinate (40 photos = 1 call)
    Used by:  available (uploader's inline flow can migrate onto it)
    Status:   BUILT

[5] job-locator.js  —  match / create a job by location
    Global:   NDJobLocator
    Origin:   Timesheet (distanceMetres / nearestJob / resolveOrCreateJob)
    Needs:    nothing (pure)
    API:      NDJobLocator.distanceMetres(la1,ln1,la2,ln2)          (pure)
              NDJobLocator.nearest(jobs, lat, lng) -> { job, dist }
              NDJobLocator.match(jobs, lat, lng, { defaultRadius, nearMult })
                -> { tier: "here"|"near"|"none", job, dist }
              NDJobLocator.resolveOrCreate(jobs, spec, make)
    Used by:  available (timesheet can migrate; feeds log-book merge +
              uploader↔planner jobs)
    Status:   BUILT

[6] sync-fill.js  —  the "bubble fills up" sync indicator
    Global:   NDSyncFill
    Origin:   Timesheet (settings danger-fill panel)
    Needs:    nothing (self-contained; injects its own CSS)
    API:      NDSyncFill.apply(el, status)   — fill any element by status
              NDSyncFill.bind(el, queue)     — apply + follow queue.onStatus
    States:   synced (full green) · pending (low) · flushing/syncing (bobbing)
              · failed (near-empty red)
    Sibling of NDUI.syncTube: the tube is a small gauge; this fills the whole
    container (button / chip / text bubble). Wraps the element's content once
    so it stays legible above the water.
    Used by:  planner (sync buttons)
    Status:   BUILT

[7] address-autocomplete.js  —  Google Places autocomplete on a field
    Global:   NDAutocomplete
    Origin:   Quote + Uploader (inline Places autocomplete)
    Needs:    Maps JS API with libraries=places (host-loaded). Pairs w/ here.js
    API:      NDAutocomplete.attach(input, { country, types, fields, bounds,
                strictBounds, onSelect })  -> autocomplete | null
              NDAutocomplete.attachAll(selector, opts)
              NDAutocomplete.victoriaBounds()
    Used by:  available (quote/uploader can migrate onto it)
    Status:   BUILT

[8] camera-capture.js  —  in-app camera modal
    Global:   NDCamera
    Origin:   Quote (in-app camera)
    Needs:    getUserMedia (all modern browsers). Optional: watermark.js
    API:      NDCamera.open({ facingMode, maxDim, jpegQ, watermarkLines,
                infoText, closeAfterCapture, onCapture(dataUrl,{w,h}),
                onClose, onError }) -> { close }
              NDCamera.isSupported()
    Notes:    Full-screen preview, flip front/back, shutter downscales the
              frame and (optionally) stamps it via NDWatermark, returns a
              JPEG data URL. Self-contained (injects its own CSS + DOM).
    Used by:  available (quote can migrate; uploader can add capture)
    Status:   BUILT

[9] drive-image.js  —  authenticated Google Drive image loader
    Global:   NDDriveImage
    Origin:   the identical fetch-with-token pattern shipped in fit-off,
              planner and search (Drive thumbnail URLs 403 without cookies).
    Needs:    a valid OAuth access token (Drive scope); fetch/URL.
    API:      NDDriveImage.url(fileId, token) -> Promise<objectURL>
              NDDriveImage.hydrate({ getToken, root }) -> Promise
                  (swaps every img[data-fileid] under root using getToken())
              NDDriveImage.clearCache()
    Used by:  planner, search  (WIRED). fit-off keeps its own loadDriveMedia
              because it also rasterises PDF floor plans.
    Status:   WIRED (planner + search)
    Notes:    Per-fileId object-URL cache, so re-renders never refetch. Render
              a tile as <img data-fileid="DRIVE_ID">, then call hydrate() after
              paint. Replaced planner's 2-call dataURL path (1 call now).

[10] loading-bar.js  —  top-of-page loading indicator
    Global:   NDLoading
    Origin:   the request for a suite-wide "scan/loading" bar (Quote keeps its
              own full-app shimmer).
    Needs:    nothing. Include the <script> before your app code.
    API:      auto — patches fetch() + XMLHttpRequest, so the slim amber bar
              shows whenever a request is in flight (gapi, Drive, Sheets, etc.)
              and hides when the last one settles. Manual too:
              NDLoading.show() / NDLoading.hide() (ref-counted) ·
              NDLoading.during(promise)
    Used by:  planner, upload, search, timesheet, fitoff, company, hub  (WIRED)
    Status:   WIRED (everywhere except quote)
    Notes:    Self-injects CSS+DOM. 30s watchdog so a hung request never sticks
              the bar. Zero per-app logic — the <script> tag is the whole wiring.

[11] remember.js  —  "remember me" / stay-signed-in store
    Global:   NDRemember
    Origin:   Quote's persist-unlock-for-the-working-day pattern, generalised.
    Needs:    localStorage.
    API:      NDRemember.save(key, data, ttlHours) · load(key) -> data|null ·
              isValid(key) · touch(key) (keep-alive) · clear(key)
    Used by:  quote (14h passcode unlock)  (WIRED)
    Status:   WIRED (quote)
    Notes:    Records store their own expiry. Google apps already "remember" via
              shared/nd-auth.js (persisted OAuth token) — this is for extra
              app-level gates like Quote's passcode. Only reads its own records,
              so it ignores unrelated localStorage keys.

[12] logo.js  —  business logo: upload / store / render + Drive sync
    Global:   NDLogo
    Origin:   built for the timesheet payslip; Quote invoices next.
    Needs:    canvas + localStorage. Drive functions need a Google token.
    API:      NDLogo.fromFile(file, {maxDim,type,quality}) -> Promise<dataUrl>
              NDLogo.attachInput(inputEl, { key, maxDim, token, ownerEmail, onChange })
              NDLogo.get/set/clear(key)   ·   NDLogo.CURRENT_KEY ("current")
              NDLogo.imgTag(dataUrlOrKey, { maxW, maxH, alt }) -> HTML
              NDLogo.pushToDrive(dataUrl, {token,ownerEmail,folderName}) -> Promise
              NDLogo.pullLatest({token,ownerEmail,folderName}) -> Promise<dataUrl|null>
              NDLogo.current({token,onUpdate}) -> cached dataUrl now + bg refresh
    Used by:  showroom hero (WIRED); to wire into timesheet payslip + quote.
    Status:   WIRED (showroom) — one shared business logo suite-wide
    Notes:    ONE logo for the whole business. A logo picked anywhere becomes the
              "current" logo: cached in localStorage (shared across every app
              same-origin, so it updates instantly) AND pushed to a "Logos"
              folder in Drive as the durable, cross-device source of truth.
              Anywhere that shows a logo reads the newest. Downscales on upload
              (480px longest edge); PNG keeps transparency. The showroom shows
              the current logo as a hero and lets you upload / sync it.

[13] doc-invoice.js  —  invoice / payslip document generator
    Global:   NDDoc
    Origin:   Quote invoice + timesheet payslip are the same document shape.
    Needs:    nothing (renders light/print HTML; prints via hidden iframe).
    API:      NDDoc.html(spec) -> markup
              NDDoc.preview(spec, { onClose }) -> on-screen modal + Print/Save
              NDDoc.print(spec) -> straight to print / save-as-PDF
    Spec:     { logo, business{name,abn,address,phone,email}, title, meta[],
                highlights[], table{columns[],rows[]}, totals[], memo, note,
                accent }
    Used by:  timesheet payslip (WIRED). Quote invoice draws NDLogo on its
              canvas header (see logo.js) rather than NDDoc, so it's not wired
              there — available if Quote ever moves to NDDoc.
    Status:   WIRED (timesheet payslip)
    Notes:    Document renders black-on-white for print regardless of app theme.
              Prints only the document (hidden iframe) — no popup blocker, the
              app UI never appears in the PDF.

[14] status-tint.js  —  tint a card by a value vs a target
    Global:   NDStatusTint
    Origin:   Timesheet interactive-times day cards (and SWB's overload card).
    Needs:    nothing (self-injects CSS).
    API:      NDStatusTint.classify(value, target, {epsilon}) -> under|exact|over
              NDStatusTint.classNames(value, target, {lowVis}) -> class string
              NDStatusTint.apply(el, value, target, {lowVis}) · clear(el)
    Used by:  timesheet (day cards)  (WIRED); showroom demo.
    Status:   WIRED (timesheet)
    Notes:    Fixed meaning across the suite: under=red, over=gold, exact=green.
              Add class "nd-tint" + a status class to any full-bordered card.
              lowVis also colours all the text in the card to match.

[15] confirm-pop.js  —  animated success tick
    Global:   NDConfirmPop
    Needs:    nothing (self-injects CSS).
    API:      NDConfirmPop.show({ label, color, size, duration, onDone }) -> Promise
    Status:   BUILT + showroom demo
    Where to wire: after an upload completes (uploader), on clock-on/off
              (timesheet), on quote/invoice save, on node placed (planner),
              on member invited (company).

[16] odometer.js  —  count-up numbers
    Global:   NDOdometer
    Needs:    nothing.
    API:      NDOdometer.to(el, value, { from, duration, decimals, prefix, suffix })
              NDOdometer.count(el, from, to, opts)
    Status:   BUILT + showroom demo
    Where to wire: quote grand total (renderTotals), timesheet week total +
              payslip Gross/Net, planner project stats, hours-on-site panel.

[17] skeleton.js  —  shimmer placeholders
    Global:   NDSkeleton
    Needs:    nothing (self-injects CSS).
    API:      NDSkeleton.lines(n, {lastWidth}) · box(w,h,r) · grid(n,{cols,tile})
              NDSkeleton.into(el, html)
    Status:   BUILT + showroom demo
    Where to wire: drop into a list/grid container before the fetch, replace with
              real content when it lands — planner project list, search photo
              grid, timesheet history, quote history. Pairs with loading-bar
              (global) for a polished load. (nd-ui also has NDUI.skeleton.)

[18] energize.js  —  "powering up" pulse (on-brand)
    Global:   NDEnergize
    Needs:    nothing (self-injects CSS).
    API:      NDEnergize.pulse(el, { color, persist })
    Status:   BUILT + showroom demo
    Where to wire: a planner node status dot when it goes Complete, fit-off
              markers when a job opens, a connection/sync dot going live.

[19] confetti.js  —  celebration confetti burst
    Global:   NDConfetti
    Needs:    nothing (creates its own canvas).
    API:      NDConfetti.burst({ duration, count, colors }) -> stop()
              NDConfetti.start(opts) / NDConfetti.stop()
    Status:   BUILT + showroom demo
    Origin:   Brandon's pixel page.  Where: quote accepted, a milestone hit.

[20] floaties.js  —  rising floating particles
    Global:   NDFloaties
    Needs:    nothing (self-injects CSS).
    API:      NDFloaties.rise(container, { emoji, count, minSize, maxSize, duration })
    Status:   BUILT + showroom demo
    Origin:   Brandon's pixel page.  Where: light celebration / "nice work" moments.

[21] pixel-cat.js  —  pixel cat, eyes follow the cursor
    Global:   NDCat
    Needs:    nothing.
    API:      NDCat.mount(el, { size }) -> { destroy } ;  NDCat.svg()
    Status:   BUILT + showroom demo
    Origin:   Brandon's pixel page.  Where: empty states, a splash/error screen mascot.

[22] dodge-button.js  —  the runaway "are you sure?" button
    Global:   NDDodge
    Needs:    nothing.
    API:      NDDodge.make(btn, { messages, grow, onGiveUp }) -> { reset }
    Status:   BUILT + showroom demo
    Origin:   Brandon's pixel page.  Where: a playful cancel / Easter egg.

[23] status-wheel.js  —  donut progress ring (NDWheel)
    Global:   NDWheel
    Origin:   Install Checklist (per-job progress) — the "status wheel" ask.
    Needs:    nothing (pure inline SVG; no deps).
    API:      NDWheel.svg({done,total,issues,na,size,stroke,label,sublabel,accent}) -> markup
              NDWheel.render(el, opts) · NDWheel.update(el, opts)
              NDWheel.percent({done,total}) -> 0..100
    Behaviour: ring fills teal->green with % complete, an amber inner arc shows
              open issues, centre reads "NN%" over "done/total", turns solid
              green at 100%. Themed to the suite; crisp at any size.
    Used by:  checklist (job cards + hero); available suite-wide (planner
              project rings, timesheet week ring, quote pipeline).
    Status:   BUILT
    Notes:    Sibling of the progress rails — the rail is linear, the wheel is
              radial. Same colour language (done green / issue amber).

------------------------------------------------------------------------
 CANDIDATE ASSETS TO EXTRACT NEXT  (best-of from the other apps)
------------------------------------------------------------------------
  (library complete — all identified best-of components extracted)
  • sync-tube              — the water-fill sync indicator now in
        shared/nd-ui.js (NDUI.syncTube) + nd-core.css. Listed here for
        discoverability; already suite-wide.

------------------------------------------------------------------------
 CHANGELOG
------------------------------------------------------------------------
  2026-07-27  Created /assets library. Added here.js and watermark.js
              (extracted from Quote). Seeded candidate list.
  2026-07-27  Added countdown-confirm.js (from Timesheet's near clock-on).
              Added the /1234567890 showroom (listed in Hub as "assets").
  2026-07-27  Wired here.js into all four apps; watermark into uploader +
              quote; built exif-geo-detect and job-locator.
  2026-07-28  Added sync-fill.js (the "bubble fills up" indicator, from
              Timesheet settings) + showroom demo; wired planner "Sync master"
              buttons onto it. Added address-autocomplete.js. Added planner
              PDF floor-plan support. Added camera-capture.js + showroom demo.
              Library now covers all identified best-of components.
  2026-07-29  Added drive-image.js (NDDriveImage) — extracted the authenticated
              Drive image loader that had been copy-pasted into fit-off, planner
              and search. Wired planner + search onto it (planner now 1 API call
              per image instead of 2). Part of the #34 reuse pass.
  2026-08-02  Added loading-bar.js (NDLoading) — suite-wide top loading bar,
              auto-instruments fetch+XHR, wired into every app except quote.
              Added remember.js (NDRemember) — reusable stay-signed-in store;
              wired Quote's 14h passcode unlock onto it.
  2026-08-02  Added logo.js (NDLogo) + doc-invoice.js (NDDoc) — the two building
              blocks for the timesheet payslip generator (and Quote logo later).
              Built standalone; wiring into apps comes next push.
  2026-08-02  logo.js gained a Drive-backed shared store: a logo uploaded
              anywhere saves to a "Logos" Drive folder and becomes the one
              current logo suite-wide (localStorage cache is shared same-origin).
              Showroom now shows the current logo as a hero + upload/sync.
  2026-08-05  Extracted 4 delights from a one-off pixel page: confetti.js
              (NDConfetti), floaties.js (NDFloaties rising particles),
              pixel-cat.js (NDCat cursor-tracking mascot), dodge-button.js
              (NDDodge runaway button). All have showroom demos.
  2026-08-02  Added 4 animations: confirm-pop.js (NDConfirmPop success tick),
              odometer.js (NDOdometer count-up), skeleton.js (NDSkeleton shimmer),
              energize.js (NDEnergize on-brand pulse). All have showroom demos.
  2026-08-02  Added status-tint.js (NDStatusTint) — extracted the Timesheet
              interactive-times day-card tint (red under / gold over / green
              bang on). Wired timesheet onto it; showroom demo added.
  2026-08-02  Wired the assets in: Timesheet payslip generator (Settings →
              Payslip estimate) uses NDDoc + NDLogo and pulls the pay period's
              logged hours. Quote invoice/quote images now draw the shared
              NDLogo in the header; logo upload added to Quote business settings.
  2026-08-19  Added status-wheel.js (NDWheel) — donut progress ring built for
              the Install Checklist's per-job status wheels; reusable suite-wide.
