# Cowork session state — Neill Data Suite Rework v2.0
Read this FIRST each session (with the two spec docs Brandon uploads:
`NeillData_Suite_Rework_v2_Cowork_Handoff.md` + `NeillPlanner_UploadSearchPlanner_Rework_Structure_v1.0.md`).
Working copy of the repo lives in the Cowork outputs folder: `toxieon.github.io-main/`.
Deliverable each session: updated `toxieon-github-io-sliceX.zip` in outputs; Brandon deploys to GitHub Pages (site: www.neilldata.com — note the www; all apps same origin).

## Status: Slices 0–5 COMPLETE (code). Remaining: #34 legacy hard-delete (deliberately ONE RELEASE after Slice 2 migration ships — do NOT do yet), Brandon #35 (keys), #36 (deploy apps-script-sendTimesheet.gs), battleship/ move (needs Brandon's destination repo). Field verification for Slices 2–5.

### Done (tasks #1–#11 of the §10 master list)
- `shared/` layer, all modules UMD (browser global + Node `module.exports`), each with a passing `*.test.js` (run `node shared/<m>.test.js`):
  - `nd-config.js` — ALL keys/IDs/folder+tab names; `forApp(id)` returns drop-in per-app config. Key swap = edit `keys` block only (Brandon task #35 pending).
  - `nd-auth.js` — shared GIS wrapper. localStorage key `nd.auth.token.v1`. Silent renew (prompt:''), consent only on interactive, proactive refresh 5min before expiry, visibilitychange re-check, cross-tab via storage event. Events: signin (once/session, incl. stored-token resume) / refresh / external / signout / error. Browser singleton `NDAuth` (init per page).
  - `nd-cache.js` — IndexedDB SWR (`NDCache.get/put/swr/refresh/onUpdate`), memoryAdapter for tests.
  - `nd-queue.js` — Timesheet queue generalised: per-app namespace `createQueue({name, ready})`, handler-per-kind, 1s debounce/30s retry/online listener, `supersede` key drop, `pending()/onStatus` for chip. Apps publish `window.NDQueue`.
  - `nd-sheets.js` — upsertRowById/appendRows/deleteRowById ONLY (no clearTab — §0.4). Queue-routed writes (kind `nd-sheets`), cache-routed reads, `gapiGateway()`.
  - `nd-ui.js` + `nd-core.css` — skeletons (`.loading-skeleton` = pixel copy of Quote's, + NDUI.skeleton variants), action toasts (undo), confirm dialog incl. typeToConfirm (danger zone ready), lightbox with regex fix (`highResUrl`), sync chip (`NDUI.syncChip(mount, queue)`), `--nd-*` tokens (#0d0d0d/amber/green/mono).
  - `nd-match.js` — Search's engine verbatim (weights frozen by test), `normalize()` = canonical addressKey (v1.0 §3.3), `rankDestinations`, `fuzzyFilter` for Planner §2.5/Hub §5.1.
  - `nd-pwa.js` + per-app `manifest.json`/`sw.js`/`icons/` for planner/upload/search/swb/quote/hub (Timesheet already had PWA, untouched).
- Wiring live: planner/upload/search index.html load shared config+auth (+nd-match in search); their app.js use `handleAuthEvent` (own initTokenClient/fetchProfile REMOVED). Search consumes NDMatch (local engine deleted). Quote: head-only changes (manifest+theme+nd-pwa) — §0.3 respected.

### Field-verified
- Suite SSO works: sign in one app → others sign in silently. Verified live in Chrome.

### Bugs found & fixed (don't reintroduce)
- SW v1 was cache-first-forever → froze a stale CDN copy of planner/app.js right after deploy ("Planner doesn't stay logged in"). Fixed: sw.js v2 = network-first navigations, stale-while-revalidate assets, install precache with {cache:'reload'}, Google APIs never cached. If an app ever looks stale: caches.delete + unregister SW + reload.
- Planner "no skeleton": expected — Planner gets skeleton boot in Slice 2 (only Quote has skeletons today).

### Gotchas
- Site canonicalises to https://www.neilldata.com (www). Same origin for all apps — SSO depends on this.
- planner assets use `?v=0.7.2` query strings — bump on deploy if needed.
- app.js files are classic scripts (not modules); shared libs loaded via <script> before them.
- The extracted `matchFile` in search/app.js now calls `NDMatch.rankDestinations` — behaviour must stay identical (tests freeze weights).
- Timesheet still on its OWN queue/auth until Slice 3/5. SWB own auth until Slice 5.

## NEXT: Slice 2 (v1.0 doc is authoritative; §6 build order), then 3, 4, 5 per §10.
Slice 2 progress is tracked below — update as you go:
- [x] #12 DONE: `shared/nd-inbox.js` (schema/lifecycle/appProps/grouping/dedup + createInboxApi + ensureInboxTab, 37 tests). Planner bootstrapDrive now ensures the tab (idempotent). Upload/Search still write the OLD pipelines — re-point in #13/#19.
- [x] #13 DONE (code + unit green; FIELD-VERIFY on iPhone): sticky header w/ per-field apply-all toggles (header wins while ON, rows grey out); exifr vendored `shared/vendor/exifr.umd.js` (75KB, MIT); EXIF GPS 5dp + DateTimeOriginal; ONE google.maps.Geocoder call per 5dp cluster (`_geocodedClusters`); header auto-fills if all photos agree (detected chip); HEIC preview via exifr.thumbnail else typed placeholder; floor-plan toggle one-per-addressGroup (auto-clears prev + toast), plans skip Type validation + `FloorPlan - <addr>` name; dedup = local + WHOLE Inbox ledger + Batch folder; uploads write Inbox row UNFILED + §5.2 appProperties (legacy keys kept for Search compat); VIC lock now NDUI.confirm "Save anyway/Fix"; Upload Log unchanged (receipt role). Upload sw cache bumped v3. NOTE: Upload still writes sheets DIRECT (no nd-queue) — offline upload queue needs IDB blob storage, deferred to #18/queue adoption.
- [x] #14 DONE: mobile 34px override deleted; @supports(cqw) block at end of planner/styles.css — container-type:size on .plan-stage, --marker-base clamp(14px,2cqw,40px), portal ×1.07; --size still via transform scale. VERIFY on a phone at 375px.
- [ ] #15 Planner delta sync (dirty-set, kill batchClear except danger-zone rebuild) + floor plans → nd-cache (IndexedDB)
- [x] #16+#17 DONE (code green; FIELD-VERIFY end-to-end: upload batch → Planner banner → Create job → plan on floor + tray opens → auto-create node from label): planner/app.js "UNIFIED INBOX" module at file end — refreshInbox (boot + focus, 15s throttle piggyback), unfiledInboxGroups (never-list np-inbox-never-v1 + session not-now), renderInboxBanners (top of Projects view, max 3), createJobFromInboxGroup (project+Ground floor pre-seeded, tagged plan linked NO re-upload via planDriveFileId + cacheFloorPlan, files moved Batch→project folder, rows FILED_TO_PROJECT, tray auto-opens), renderSortTrayModal (modal mode "sort-tray"; To sort (n) chip in topbar), fileInboxToNode (move to node folder + imageRefs push → delta sync writes Photos, status FILED_TO_NODE), autoCreateNodeFromRecord (tidy stack nextAutoNodePosition: x=50+col*6 wrap 6/row, y=8+row*6, node.autoPlaced flag), dismissInboxToBatch (UNFILED + move back to Batch), delegated click listener (data-inbox-*/data-tray-*). CSS at end of planner/styles.css.
- [x] #18 DONE: planner uploadBulkPhotos re-pointed → unified pipeline (in-project → project folder + FILED_TO_PROJECT + tray opens; else Batch + UNFILED); migrateLegacyBulkPhotos() one-time (flag np-legacy-bulk-migrated-v1): unallocated Bulk Photos → Batch + Inbox rows; Photo-Allocation/Bulk Photos now READ-ONLY (legacy picker kept for safety; appendPhotoAllocationRows only reachable via legacy assign). Hard-delete = #34, one release later.
- [x] #19 DONE: search reads Inbox tab (loadInboxRecords → inboxRecordToFile; UNFILED/UPLOADED/FILED_TO_PROJECT; Upload Log demoted to receipt), cached warm boot via NDCache "search:index" (§2.8), move flow removes REAL old parents (works for project-folder files) + graduates Inbox row FILED_TO_NODE, delete cleans Inbox row, all 3 confirms → NDUI.confirm, lightbox → shared NDUI.lightbox. NOTE: loadBatchFiles/loadUploadRows/enrichFile now dead code — remove in #31. search sw v3.
- [x] #20 DONE: undoableDelete pattern (local removal instant, Drive-trash DEFERRED to toast expiry, Undo restores + delta re-upserts) for node/room/floor delete; photo-unfile n/a (tray dismiss covers it); 11 native confirms → NDUI.confirm (danger-styled; wipe-local + format = typeToConfirm WIPE/FORMAT); settings nine-button row → labelled routine rows + red Danger zone section (rebuild/format); §2.5 projects fuzzy search (NDMatch.fuzzyFilter over name/address/node/room, debounced focus-preserving input; node list keeps existing query filter).
- [x] #21 PASS: audited all wholesale writes — planner rebuild/format (danger-zone, Inbox excluded), planner Categories clear (single-writer own tab), swb per-project sheets (single-writer), timesheet settings tab (own spreadsheet), search Photos row clear (keyed single row). Shared tabs (Inbox/Photos) all keyed via nd-inbox/nd-sheets or snapshot-scoped deltas. SLICE 2 COMPLETE.
Resolved decisions (handoff §2.1): exifr; geocode capped/deduped; tidy stack; legacy read-only 1 release; VIC lock stays but visible warning.

### Session log
- S1: Slices 0+1 built+tested (175 tests). S1b: SSO verified in browser; SW strategy bug found+fixed (v2).
- S2: state doc; #14 marker fix; #12 nd-inbox.js + tests + Planner ensures tab.
- S3: #13 Upload rework done (see above). NEXT: #15 Planner delta sync (kill the clobber — dirty-set model, §2.2) + floor plans→IndexedDB (§2.3); then #16 banner + #17 tray (both read Inbox via NDInbox.groupUnfiled which is ready).
### Slice 3 DONE (all in timesheet/index.html unless noted; FIELD-VERIFY on phone):
- #22 +Add shift: openAddShift(dateIso) reuses detail modal (date field added, defaults standardClockOn/Off + defaultBreakMin, entry_method:'manual', OT recalc, queueSheetWrite); "+" button on each day-card head + FAB on History (bottom-right above tabbar).
- #23 AI-prompt copier fully removed (button/const/listener). CSV import + template kept.
- #24 Tabs 4→3 (Clock·History·Settings): export view/module deleted; export = <details id=export-panel> at bottom of History sharing weekCursor (preview renders on open); print CSS repointed to #view-history .export-panel.
- #25 sendViaBackend(): POST text/plain JSON {action:'sendTimesheet',to,cc,subject,body} → backendUrl (settings override or TIMESHEET_CONFIG.neillQuote.backendUrl); saves lastSent/lastSentWeek; primary Send btn = backend, "Open in Mail app instead" = old mailto path. Apps Script written: timesheet/apps-script-sendTimesheet.gs (Brandon deploys into the existing Quote backend project, task #36). Send-day nudge: maybeShowSendNudge() card on clock screen (sendDay + week has sessions + lastSentWeek≠this week + not dismissed 'ts_nudge_dismissed'), hooks: boot + visibilitychange; Preview opens History panel, Send→backend.
- #26 Today chip (wk-today, hidden on current week); period totals honour payPeriodType (Fortnightly/Monthly window from payPeriodStartDay) + last-4-weeks hours strip (#period-summary); flexible breaks: break_minutes field (chips 0/30/60) in edit + add modals, settings defaultBreakMin (s-defaultBreakMin), netHours(minutes) w/ legacy bool→30 compat, sessionBreakMinutes(), sheet headers gained break_minutes + entry_method (ensureTabs adds tabs but NOT new columns on existing sheets — Brandon may need to add the two columns to sessions tab manually OR fine since writeRow maps by header; VERIFY a manual shift row lands with break/entry values).
- #27 NDUI.accordion(root,{sectionSelector,titleSelector,storageKey,danger[]}) added to shared/nd-ui.js + .nd-acc CSS in nd-core.css — THE §3.6 pattern for Quote #29. Timesheet settings init wraps .settings-section blocks (collapsed by default, remembers open, "More" = danger-styled). Timesheet loads nd-core.css + nd-ui.js; its sw.js ported to v2 strategy (cache timesheet-v3, shell incl. shared assets).
- NOTE: Timesheet still runs its ORIGINAL inline queue/auth — nd-queue/nd-auth adoption deferred to #33 (semantics identical; low-risk swap later).
### Slice 4 DONE (quote/index.html; §0.3 respected — UI changes ONLY in Settings tab):
- #28: Quote has NO Google OAuth — its auth = passcode+fingerprint backend login. "Never asks to sign in mid-day" implemented as 14h unlock session (nq_session_v1: saved on unlock, restored at boot via tryRestoreSession() with silent background re-verify; locked account wipes+reloads; network failure stays unlocked). Writes: backendCall split → backendCallDirect + queue wrapper: NETWORK failures on save_quote/delete_quote/set_status/set_favourites enqueue to nd-queue 'quote' (handler 'backend' replays; delete supersedes queued saves by id; ready()=currentUser set; window.NDQueue published). Price CSVs: cachedCsvFetch = network-first + NDCache fallback (key quote:csv:<url>) — offline sync serves last good copy. Skeleton CSS block REMOVED from quote/index.html — consumes shared .loading-skeleton from nd-core.css (pixel-identical via var fallbacks). Head loads nd-core.css + nd-cache/nd-queue/nd-ui. quote sw v3.
- #29: Settings tab (§3.6 pattern, same behaviours as Timesheet): all cards collapsed by default (Business info lost `open`), open state remembered (nq-settings-acc) via wireSettingsAccordion() called from renderSettings(), keyboard-aware focusin scrollIntoView, 44px inputs full-width + grid-2 → 1 column on ≤640px, danger tint on cards containing .btn-danger (Activity log). All CSS scoped [data-panel="settings"].
### Slice 5 DONE:
- #30 Hub: crawler GONE — hub/apps.json registry (7 entries incl hidden site), hub/app.js rewritten ~100 lines (fuzzy filter via NDMatch, recents row), index.html shell simplified (blacklist/bulk/settings UI removed), hub sw v3 precaches apps.json. Continue-where-you-left-off: NDUI.recordVisit/recentVisits in nd-ui.js (localStorage nd:recent:v1 + NDCache mirror, cap 12, dedup app+context); writers wired: planner project open (deep link /planner/#project=<id> — hydrateFromHash now supports project=), timesheet clock-off (Shift: <job>), upload batch complete. Quote NOT wired (§0.3).
- #31: deleted battleship/indexold.html, idkoldver.html, swb/_s, basic-electrical-wiring-guidelines.jpg (no dangling refs). 404.html at root: ND-styled, home link + tel:0418 912 681. battleship/ move PENDING Brandon's destination repo.
- #32: NDUI.batchFill in nd-ui.js (copy-fields-to-all + deep-list-copy modes); SWB circuit modal gained "Batch fill — copy devices from another circuit" select (fills _modalDevices via batchFill, toast); planner Projects view Recent chips row (proj.lastOpenedAt, top 4); timesheet manual job picker sorted last_visit desc (jobs sheet header + clockOff stamp).
- #33 adoption pass: SWB → nd-auth (handleAuthEvent pattern, googleCall renews via ensureToken, config from forApp("swb"), loads nd-auth/nd-ui/nd-core.css, sw v3); Timesheet → nd-auth (TsSheets onAuthChange bridge, signIn/signOut via NDAuth, creds/scopes overridden from forApp("timesheet") — scope union now incl. full drive) + nd-core.css/nd-ui already in. NOTE: Timesheet + Quote KEEP their own internal queues (Timesheet's IS the nd-queue reference implementation; Quote's wraps nd-queue for backend writes) — sync CHIP component not mounted in swb/timesheet (they have their own status dots); acceptable per suite behaviour, revisit only if Brandon wants visual consistency.
- SUITE-WIDE ACCEPTANCE now testable (§11): one sign-in per day across Planner/Upload/Search/SWB/Timesheet (same localStorage token); photo with no signal → node without laptop; all apps installable PWAs.
- Field checks pending for Brandon: markers on phone; Inbox tab appears in master sheet; upload a batch → header autofill from GPS, HEIC previews, floor-plan flag, Inbox rows land UNFILED.
